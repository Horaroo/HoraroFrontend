name: Deploy-staging

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-22.04
    steps:
      - name: deploy
        uses: appleboy/ssh-action@master
        with:
          host: horaro.net
          username: ubuntu
          key: ${{ secrets.AWS_HORARO_PRYVATE_KEY }}
          script: | 
                  cd ~/prod_horaro/HoraroFrontend/ &&\
                  git pull && cd .. &&\ 
                  make up_and_build_one_service service=front
          